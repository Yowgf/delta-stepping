# delta-stepping project Makefile
# Main global variables custom definitions
# ==============================================================================
# Everything in this configuration file has to do with the main (default build)
#   target.
# ------------------------------------------------------------------------------

SOURCE_MAIN := 

HEADER_MAIN :=
APPLIANCE_MAIN := 

BUILD_MAIN := 

MAIN_FILE = tools/main.cpp
TARGET = $(BUILD)/delta-stepping
